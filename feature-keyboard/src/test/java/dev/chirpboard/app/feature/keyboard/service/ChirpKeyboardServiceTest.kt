package dev.chirpboard.app.feature.keyboard.service

import org.junit.Test
import org.junit.Assert.assertTrue

class ChirpKeyboardServiceTest {
    @Test
    fun `initializes correctly`() {
        assertTrue(true)
    }
}
