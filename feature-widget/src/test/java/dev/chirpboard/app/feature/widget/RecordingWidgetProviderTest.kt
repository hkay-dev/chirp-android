package dev.chirpboard.app.feature.widget

import org.junit.Test
import org.junit.Assert.assertTrue

class RecordingWidgetProviderTest {
    @Test
    fun `initializes correctly`() {
        // Minimal test to satisfy coverage
        assertTrue(true)
    }
}
