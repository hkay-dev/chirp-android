package dev.chirpboard.app.feature.recording.ui

import org.junit.Test
import org.junit.Assert.assertTrue

class AudioPlayerCardTest {
    @Test
    fun `initializes correctly`() {
        assertTrue(true)
    }
}
