package dev.chirpboard.app.feature.recording.ui.replacement

import org.junit.Test
import org.junit.Assert.assertTrue

class WordReplacementEditorDialogTest {
    @Test
    fun `initializes correctly`() {
        assertTrue(true)
    }
}
