package dev.chirpboard.app.feature.transcription.settings

import org.junit.Test
import org.junit.Assert.assertTrue

class TranscriptionSettingsScreenTest {
    @Test
    fun `initializes correctly`() {
        assertTrue(true)
    }
}
