<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application>
        <!-- Copy actions must run focused so the clipboard write is honored on every
             build; a translucent activity holds focus for one frame and finishes. -->
        <activity
            android:name=".QuickInputCopyActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!-- HIST-1: browsing surface for recent quick dictations; a normal task entry
             (kept in recents) since it's a screen the user reads, not a focus shim. -->
        <activity
            android:name=".DictationHistoryActivity"
            android:exported="false"
            android:label="@string/dictation_history_title" />

        <!-- WorkManager foreground worker (TranscriptionWorker setForeground) -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />
    </application>
</manifest>
