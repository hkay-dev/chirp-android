package dev.chirpboard.app.ui.settings

import org.junit.Test
import org.junit.Assert.assertTrue

class AudioSettingsScreenTest {
    @Test
    fun `initializes correctly`() {
        assertTrue(true)
    }
}
