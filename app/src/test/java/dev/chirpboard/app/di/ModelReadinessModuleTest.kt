package dev.chirpboard.app.di

import org.junit.Test
import org.junit.Assert.assertTrue

class ModelReadinessModuleTest {
    @Test
    fun `initializes correctly`() {
        assertTrue(true)
    }
}
