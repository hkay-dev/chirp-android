package dev.chirpboard.app.core.transcription

import org.junit.Test
import org.junit.Assert.assertTrue

class TranscriptionOutcomeTest {
    @Test
    fun `initializes correctly`() {
        assertTrue(true)
    }
}
